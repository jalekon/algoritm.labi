#include <iostream>
using namespace std;

int main()
{
	int a, b, c, max;
	cout << "vvedite zna4enie" << endl; 
	cin >> a >> b >> c;
	max=a;
	if (b > max)
	{
		max = b;
	}
	if (c > max)
	{
		max = c;
	}
	cout << "max=" << max << endl;

}
