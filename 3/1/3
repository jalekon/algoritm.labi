using namespace std;
#include <iostream> 

int main()
{
	float N, i, s;
	i = 2;
	s = 1;
	cout << "vvedite N " << endl;
	cin >> N;
	while (i <= N)
	{
		s = s * (1 + (1 / (i * i)));
		i++;
	}
	cout << "2 v stepeni N = " << s;
}
